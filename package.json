{
  "name": "agent-sla-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AgentSLA DSL, SDKs, API facade, MCP server, and Codex skill.",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/sdk-js",
    "apps/api",
    "apps/mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
