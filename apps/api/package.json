{
  "name": "@agent-sla/api",
  "version": "0.1.0",
  "description": "HTTP API facade for AgentSLA.",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@agent-sla/core": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
